#!/usr/bin/expect -f
set timeout 60
set user "nbhtd"
set host "hellbender.rnet.missouri.edu"
set password "Grad123!\$"
set remote_dir "~/melsi_simulations/hellbender"

spawn scp "${user}@${host}:${remote_dir}/table3_scalability_summary.csv" .

expect {
    "password:" {
        send "$password\r"
        exp_continue
    }
    eof
}
